#include<string.h>
#include<stdio.h>
void main()
{
 char pwd[10];
 char ch;
 int i=0;
 printf("enter your password");
 while((ch=getch())!=13)
 {
     pwd[i]=ch;
     printf("*");
     i+=1;
 }
 printf("the password is %d",pwd);
}
