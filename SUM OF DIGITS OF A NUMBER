#include<stdio.h>
void main()
{
    int n,sum=0,c;
    printf("enter a number");
    scanf("%d",&n);
    while(n>0)
    {
      c=n%10;
      sum+=c;
      n=n/10;
    }
    printf("sum of digits is %d",sum);
}
