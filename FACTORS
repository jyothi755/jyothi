#include <stdio.h>
void main() 
{
    int num;
    printf("enter the  value");
    scanf("%d",&num);
    for(int i=2;i<=num;i++)
    {
        if(num%i==0)
        {
        printf("%d",i);
        printf("\n");
        }
    }
}
